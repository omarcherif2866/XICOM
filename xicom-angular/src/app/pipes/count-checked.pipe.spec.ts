import { CountCheckedPipe } from './count-checked.pipe';

describe('CountCheckedPipe', () => {
  it('create an instance', () => {
    const pipe = new CountCheckedPipe();
    expect(pipe).toBeTruthy();
  });
});
